#
add_subdirectory(utils)
# 
# # Custom targets for submodules
# add_custom_target(utils_code
#     COMMAND ${CMAKE_COMMAND} -E echo "<**Building utils directory**>"
#     COMMAND make -C ${CMAKE_SOURCE_DIR}/utils all
# )
# 
